## Site shards

[About](<%= ENV["SITE_BASE_URL"] %>/about) ∷
a short bio

[Resume](<%= ENV["SITE_BASE_URL"] %>/resume) ∷
professional background and relevant skills

[Projects](<%= ENV["SITE_BASE_URL"] %>/projects) ∷
what I create in my free time

[GitHub](https://github.com/valera-rozuvan) ∷
my source code that lives on GitHub

[Contact](<%= ENV["SITE_BASE_URL"] %>/contact) ∷
want to connect? let's chat

[GPG Key](<%= ENV["SITE_BASE_URL"] %>/gpg-key) ∷
sign commits, encrypt messages, be security-minded
